{
  "name": "spec-portals",
  "displayName": "spec-portals",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.102.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:extension.viewConfig"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "extension.viewConfig",
        "title": "📦 View Config Contents"
      }
    ]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@types/vscode": "^1.102.0",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2",
    "eslint": "^9.25.1"
  },
  "dependencies": {
    "@redocly/cli": "^1.34.4",
    "yaml": "^2.8.0"
  }
}
